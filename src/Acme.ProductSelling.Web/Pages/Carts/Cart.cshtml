@page "/cart"
@model Acme.ProductSelling.Web.Pages.Carts.CartModel

@{
    ViewData["Title"] = L["Cart:ShoppingCart"];
    Layout = "/Views/Shared/_Layout.cshtml";
}
@section scripts {
    <abp-script src ="/Pages/Carts/Index.js" />
}
@section styles {
    <style>
        /* Cart Page Styles */

        .quantity-input {
            max-width: 60px;
        }

            .quantity-input::-webkit-inner-spin-button,
            .quantity-input::-webkit-outer-spin-button {
                -webkit-appearance: none;
                margin: 0;
            }

        .quantity-input {
            -moz-appearance: textfield;
        }

        .table > :not(caption) > * > * {
            vertical-align: middle;
        }

        .btn-decrease,
        .btn-increase {
            padding: 0.25rem 0.5rem;
        }

            .btn-decrease:hover,
            .btn-increase:hover {
                background-color: #f8f9fa;
            }

        /* Empty cart styling */
        .fa-shopping-cart.fa-3x {
            font-size: 4rem;
        }

        /* Responsive adjustments */
        @@media (max-width: 768px) {
            .table td, .table th {
                font-size: 0.875rem;
                padding: 0.5rem;
            }

            .input-group {
                width: 110px !important;
            }

            .quantity-input {
                max-width: 50px;
            }

            .btn-decrease,
            .btn-increase {
                padding: 0.2rem 0.4rem;
                font-size: 0.75rem;
            }
        }

        /* Loading state */
        .quantity-input:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

        /* Cart total styling */
        tfoot .text-primary {
            color: #0d6efd !important;
            font-weight: 700;
        }

        /* Button styling */
        .btn-outline-danger:hover {
            color: #fff;
            background-color: #dc3545;
            border-color: #dc3545;
        }

        /* Product link styling */
        a.text-decoration-none:hover {
            text-decoration: underline !important;
        }

        /* Card styling adjustments */
        .abp-card-body {
            padding: 1.5rem;
        }

        @@media (max-width: 576px) {
            .d-flex.justify-content-between {
                flex-direction: column;
                gap: 1rem;
            }

                .d-flex.justify-content-between > div {
                    width: 100%;
                    text-align: center;
                }

                .d-flex.justify-content-between .btn {
                    width: 100%;
                }
        }
    </style>
}
<div class="container mt-4 mb-5">
    <h1>@L["Cart:ShoppingCart"]</h1>


    @if (Model.Cart != null && Model.Cart.CartItems.Any())
    {
        <div class="table-responsive">
            <table class="table align-middle">
                <thead>
                    <tr>
                        <th scope="Column" style="width: 35%;">@L["Cart:Product"]</th>
                        <th scope="Column" style="width: 15%;" class="text-end">@L["Cart:Price"]</th>
                        <th scope="Column" style="width: 15%;" class="text-center">@L["Cart:Quantity"]</th>
                        <th scope="Column" style="width: 15%;" class="text-end">@L["Cart:Total"]</th>
                        <th scope="Column" style="width: 10%;" class="text-center">@L["Cart:Actions"]</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Cart.CartItems)
                    {
                        <tr>

                            <td>
                                <a asp-page="/Products/ProductDetail"
                                   asp-route-slug="@item.ProductUrlSlug" class="text-dark text-decoration-none fw-bold">
                                    @item.ProductName
                                </a>
                                @* Có thể thêm các thông tin khác như thuộc tính sản phẩm nếu cần *@
                            </td>
                            <td class="text-end">@item.ProductPrice.ToString("C0", CultureInfo.GetCultureInfo("vi-VN"))</td>
                            <td class="text-center">
                                <form method="post" asp-page-handler="UpdateItem" class="d-inline">
                                    <input type="hidden" name="cartItemId" value="@item.Id" />
                                    <div class="input-group input-group-sm" style="width: 130px; margin: 0 auto;">
                                        <button type="button" class="btn btn-outline-secondary btn-decrease"
                                                data-item-id="@item.Id"
                                                data-current-quantity="@item.Quantity">
                                            <i class="fas fa-minus"></i>
                                        </button>
                                        <input type="number" name="quantity"
                                               class="form-control text-center quantity-input"
                                               value="@item.Quantity"
                                               min="1"
                                               max="100"
                                               data-item-id="@item.Id" />
                                        <button type="button" class="btn btn-outline-secondary btn-increase"
                                                data-item-id="@item.Id"
                                                data-current-quantity="@item.Quantity">
                                            <i class="fas fa-plus"></i>
                                        </button>
                                    </div>
                                </form>
                            </td>
                            <td class="text-end fw-bold">@item.LineTotal.ToString("C0", CultureInfo.GetCultureInfo("vi-VN"))</td>
                            <td class="text-end align-middle">
                                <button type="button" class="btn btn-sm btn-outline-danger btn-remove-item"
                                        data-item-id="@item.Id"
                                        data-product-name="@item.ProductName">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="row mt-4">
            <div class="col-md-6 offset-md-6">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">@L["Cart:Summary"]</h5>
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                @L["Cart:Subtotal"]
                                <span>@Model.Cart.TotalPrice.ToString("C0", CultureInfo.GetCultureInfo("vi-VN"))</span>
                            </li>
                            <li class="list-group-item d-flex justify-content-between align-items-center fw-bold fs-5">
                                @L["Cart:Total"]
                                <span>@Model.Cart.TotalPrice.ToString("C0", CultureInfo.GetCultureInfo("vi-VN"))</span> @* Tạm thời chỉ có Subtotal *@
                            </li>
                        </ul>
                        <div class="d-grid gap-2 mt-3">
                            <a asp-page="/Orders/Checkout" class="btn btn-primary btn-lg">@L["Cart:ProceedToCheckout"]</a>
                            <a asp-page="/Index" class="btn btn-outline-secondary">@L["ContinueShopping"]</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="alert alert-info text-center">
            <p>@L["Cart:YourCartIsEmpty"]</p>
            <a asp-page="/Index" class="btn btn-primary">@L["StartShopping"]</a>
        </div>
    }
</div>
@* <abp-card>
    <abp-card-header>
        <abp-row>
            <abp-column size-md="_8">
                <h2>@L["Cart:ShoppingCart"]</h2>
            </abp-column>
            <abp-column size-md="_4" class="text-end">
                @if (Model.CartItems.Any())
                {
                    <span class="badge bg-primary">@Model.TotalItemCount @L["Cart:Items"]</span>
                }
            </abp-column>
        </abp-row>
    </abp-card-header>

    <abp-card-body>
        @if (!Model.CartItems.Any())
        {
            <div class="text-center py-5">
                <i class="fas fa-shopping-cart fa-3x text-muted mb-3"></i>
                <h4>@L["Cart:EmptyCart"]</h4>
                <p class="text-muted">@L["Cart:EmptyCartMessage"]</p>
                <a href="/" class="btn btn-primary">@L["Cart:ContinueShopping"]</a>
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>@L["Cart:Product"]</th>
                            <th class="text-center">@L["Cart:Price"]</th>
                            <th class="text-center">@L["Cart:Quantity"]</th>
                            <th class="text-end">@L["Cart:Total"]</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.CartItems)
                        {
                            <tr>
                                <td>
                                    <div class="d-flex align-items-center">
                                        <div>
                                            <a href="/products/@item.ProductUrlSlug" class="fw-bold text-decoration-none">
                                                @item.ProductName
                                            </a>
                                        </div>
                                    </div>
                                </td>
                                <td class="text-center align-middle">
                                    <span class="text-primary fw-bold">@item.ProductPrice.ToString("N0") ₫</span>
                                </td>
                                <td class="text-center align-middle">
                                    <form method="post" asp-page-handler="UpdateItem" class="d-inline">
                                        <input type="hidden" name="cartItemId" value="@item.Id" />
                                        <div class="input-group input-group-sm" style="width: 130px; margin: 0 auto;">
                                            <button type="button" class="btn btn-outline-secondary btn-decrease"
                                                    data-item-id="@item.Id"
                                                    data-current-quantity="@item.Quantity">
                                                <i class="fas fa-minus"></i>
                                            </button>
                                            <input type="number" name="quantity"
                                                   class="form-control text-center quantity-input"
                                                   value="@item.Quantity"
                                                   min="1"
                                                   max="100"
                                                   data-item-id="@item.Id" />
                                            <button type="button" class="btn btn-outline-secondary btn-increase"
                                                    data-item-id="@item.Id"
                                                    data-current-quantity="@item.Quantity">
                                                <i class="fas fa-plus"></i>
                                            </button>
                                        </div>
                                    </form>
                                </td>
                                <td class="text-end align-middle">
                                    <span class="fw-bold">@item.LineTotal.ToString("N0") ₫</span>
                                </td>
                                <td class="text-end align-middle">
                                    <button type="button" class="btn btn-sm btn-outline-danger btn-remove-item"
                                            data-item-id="@item.Id"
                                            data-product-name="@item.ProductName">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr class="table-light">
                            <td colspan="3" class="text-end fw-bold">@L["Cart:TotalPrice"]:</td>
                            <td class="text-end">
                                <h4 class="text-primary mb-0">@Model.TotalPrice.ToString("N0") ₫</h4>
                            </td>
                            <td></td>
                        </tr>
                    </tfoot>
                </table>
            </div>

            <div class="d-flex justify-content-between mt-4">
                <div>
                    <a href="/" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left"></i> @L["Cart:ContinueShopping"]
                    </a>
                    <button type="button" class="btn btn-outline-danger ms-2" id="btnClearCart">
                        <i class="fas fa-trash-alt"></i> @L["Cart:ClearCart"]
                    </button>
                </div>
                <div>
                    <a href="/checkout" class="btn btn-primary btn-lg">
                        @L["Cart:ProceedToCheckout"] <i class="fas fa-arrow-right"></i>
                    </a>
                </div>
            </div>
        }
    </abp-card-body>
</abp-card> *@