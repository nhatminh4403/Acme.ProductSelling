@page "/{prefix}/users"

@inject IAuthorizationService AuthorizationService
@model Acme.ProductSelling.Web.Pages.Admin.Users.IndexModel

@{
    ViewData["Title"] = L["Title:UserManagement"];
    Layout = "/Pages/Admin/Shared/_AdminLayout.cshtml";
}

@section scripts {
    <abp-script src="/Pages/Admin/Users/Index.js" />
}

<abp-card>
    <abp-card-header>
        <h2>@L["Admin:User:UserManagement"]</h2>
    </abp-card-header>
    <abp-card-body>
        <abp-tabs name="UsersTabs">
            <abp-tab title="@L["Admin:User:StaffUsers"].Value" name="Staff">
                <abp-table striped-rows="true" id="StaffUsersTable" class="nowrap"></abp-table>
            </abp-tab>
            <abp-tab title="@L["Admin:User:UnassignedStaff"].Value" name="Unassigned">
                <abp-table striped-rows="true" id="UnassignedStaffTable" class="nowrap"></abp-table>
            </abp-tab>
        </abp-tabs>
    </abp-card-body>
</abp-card>

<!-- Assign Store Modal -->
<abp-modal id="assignStoreModal" static="true" size="Default">
    <abp-modal-header title="@L["Admin:User:AssignToStore"].Value"></abp-modal-header>
    <abp-modal-body>
        <form id="assignStoreForm">
            <input type="hidden" id="userId" />
            <div class="mb-3">
                <label class="form-label">@L["Admin:User:UserName"]</label>
                <input type="text" class="form-control" id="userName" readonly />
            </div>
            <div class="mb-3">
                <label class="form-label">@L["Admin:User:Email"]</label>
                <input type="text" class="form-control" id="userEmail" readonly />
            </div>
            <div class="mb-3">
                <label class="form-label">@L["Admin:User:Roles"]</label>
                <input type="text" class="form-control" id="userRoles" readonly />
            </div>
            <div class="mb-3">
                <label class="form-label">@L["Admin:User:Store"] *</label>
                <select class="form-select" id="storeId" required>
                    <option value="">-- @L["Admin:User:SelectStore"] --</option>
                </select>
            </div>
        </form>
    </abp-modal-body>
    <abp-modal-footer buttons="@(AbpModalButtons.Save | AbpModalButtons.Cancel)"></abp-modal-footer>
</abp-modal>